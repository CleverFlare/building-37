[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["SKIP_ENV_VALIDATION=1 npm run build"]
dependsOn = ["install"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
